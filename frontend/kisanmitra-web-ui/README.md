# KisanMitraAI Web Tool Login UI

Clean production web login screen for `frontend/kisanmitra-web-ui`.

V2 fixes:
- Keeps the 1672 x 941 final UI aspect ratio.
- Scales every overlay element with the image stage, so the login card does not get oversized or clipped on browser screens.
- Keeps the background, logo, card, footer strip and form positions locked to the final design.

Open:
`frontend/kisanmitra-web-ui/index.html`
